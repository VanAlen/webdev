{% extends 'base.html.twig' %}

{% block title %}Jewelrytype{% endblock %}

{% block body %}
    <h1>Jewelrytype</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ jewelrytype.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ jewelrytype.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_jewelrytype_index') }}">back to list</a>

    <a href="{{ path('app_jewelrytype_edit', {'id': jewelrytype.id}) }}">edit</a>

    {{ include('jewelrytype/_delete_form.html.twig') }}
{% endblock %}
