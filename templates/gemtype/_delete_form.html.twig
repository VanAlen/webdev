<form method="post" action="{{ path('app_gemtype_delete', {'id': gemtype.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gemtype.id) }}">
    <button class="btn">Delete</button>
</form>
