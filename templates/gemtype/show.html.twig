{% extends 'base.html.twig' %}

{% block title %}Gemtype{% endblock %}

{% block body %}
    <h1>Gemtype</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gemtype.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ gemtype.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_gemtype_index') }}">back to list</a>

    <a href="{{ path('app_gemtype_edit', {'id': gemtype.id}) }}">edit</a>

    {{ include('gemtype/_delete_form.html.twig') }}
{% endblock %}
